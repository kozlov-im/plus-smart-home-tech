spring:
  datasource:
    url: jdbc:postgresql://localhost:8432/smart-home-cart
    driverClassName: org.postgresql.Driver
    username: sa
    password: sa
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create #none
    properties:
      hibernate:
        format_sql: true
  application:
    name: shopping-cart

server:
  port: 0
spring:
  datasource:
    url: jdbc:postgresql://localhost:7432/smart-home-shop
    driverClassName: org.postgresql.Driver
    username: sa
    password: sa
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create #none
    properties:
      hibernate:
        format_sql: true
  application:
    name: shopping-store

server:
  port: 0